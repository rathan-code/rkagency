<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mirapakaya Media | Creative Production & Marketing</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        /* --- CSS VARIABLES & RESET --- */
        :root {
            /* REFINED MONOCHROMATIC PALETTE */
            --primary-red: #ff173a;       /* Deep Crimson */
            --primary-dark: #9c0015;      
            --bg-deep-red: #d42929;       
            
            --pure-white: #ffffff;
            --off-white: #f8f8f8;
            --pure-black: #0a0a0a;
            --dark-grey: #1f1f1f;
            --text-grey: #444444;

            /* MAPPING */
            --primary-color: var(--primary-red);
            --secondary-color: var(--pure-white);
            --accent-color: var(--pure-white);
            
            --text-main: var(--pure-black);
            --text-light: var(--pure-white);
            --text-muted: var(--text-grey);
            
            --border-color: rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; }
        html { scroll-behavior: smooth; }
        body { background-color: var(--secondary-color); color: var(--text-main); line-height: 1.6; overflow-x: hidden; }
        a { text-decoration: none; color: inherit; }
        ul { list-style: none; }

        /* --- NAVIGATION --- */
        header {
            position: fixed; top: 0; width: 100%;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            z-index: 1000;
            border-bottom: 1px solid var(--border-color);
            box-shadow: 0 2px 15px rgba(0,0,0,0.03);
        }
        nav { max-width: 1200px; margin: 0 auto; display: flex; justify-content: space-between; align-items: center; padding: 1rem 2rem; }
        
        .logo { font-size: 1.6rem; font-weight: 800; letter-spacing: 0.5px; color: var(--pure-black); display: flex; align-items: center; gap: 8px; }
        .logo i { color: var(--primary-color); transform: rotate(-20deg); font-size: 1.4rem; }
        .logo span { color: var(--primary-color); }
        
        .nav-links { display: flex; gap: 2rem; }
        .nav-links a { font-size: 1rem; font-weight: 600; transition: var(--transition); color: var(--dark-grey); }
        .nav-links a:hover { color: var(--primary-color); }
        .menu-toggle { display: none; font-size: 1.5rem; cursor: pointer; color: var(--pure-black); }

        /* --- HERO SECTION --- */
        .hero {
            height: 100vh; display: flex; align-items: center; justify-content: center; text-align: center;
            padding: 0 2rem; background-color: var(--pure-white); position: relative; overflow: hidden; margin-top: 0;
        }
        
        .hero::before {
            content: ""; position: absolute; top: 0; left: 0; width: 100%; height: 100%;
            background-image: linear-gradient(rgba(0, 0, 0, 0.07) 1px, transparent 1px), linear-gradient(90deg, rgba(0, 0, 0, 0.03) 1px, transparent 1px);
            background-size: 40px 40px; opacity: 1; z-index: 1; pointer-events: none;
        }

        /* --- SPOTLIGHTS --- */
        .hero-spotlights { position: absolute; top: 0; left: 0; width: 100%; height: 100%; overflow: hidden; z-index: 0; }
        .spotlight { position: absolute; border-radius: 50%; filter: blur(100px); opacity: 0.3; animation: moveSpotlight 15s infinite alternate ease-in-out; }
        
        .spotlight-1 { width: 600px; height: 600px; background: var(--primary-red); top: -20%; left: -10%; opacity: 0.50; }
        .spotlight-2 { width: 500px; height: 500px; background: #ff2f2f; bottom: -20%; right: -10%; animation-delay: -5s; animation-duration: 20s; opacity: 0.40; }
        
        @keyframes moveSpotlight {
            0% { transform: translate(0, 0) scale(1); }
            50% { transform: translate(30px, 20px) scale(1.1); }
            100% { transform: translate(-20px, -30px) scale(0.9); }
        }

        /* --- FLOATING ICONS (Global) --- */
        .bg-animated-icons { position: absolute; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; z-index: 1; overflow: hidden; }
        
        .bg-icon { 
            position: absolute; 
            opacity: 0.1; /* Low opacity for background elements */
            font-size: 3rem; 
            animation: floatIcon 15s infinite linear; 
            color: var(--pure-black); 
        }
        
        /* Specific Styles for Adobe Boxes */
        .adobe-box { 
            font-family: sans-serif; font-weight: 900; 
            display: flex; align-items: center; justify-content: center; 
            width: 80px; height: 80px; 
            font-size: 2.5rem !important; 
            border-radius: 12px; 
            opacity: 0.6;
            background-color: #00005b; 
        }
        .adobe-pr { color: #d8a0ff; }
        .adobe-ae { color: #d8a0ff; } /* Ae and Pr utilize similar branding colors */

        /* White Icons for Dark/Red Backgrounds */
        .section-dynamic-bg .bg-icon, 
        .section-dark-bg .bg-icon {
            color: var(--pure-white);
            opacity: 0.1;
        }
        .section-dynamic-bg .adobe-box { border: 1px solid white; box-shadow: 0 5px 15px rgba(0,0,0,0.2); }

        @keyframes floatIcon {
            0% { transform: translateY(120px) rotate(0deg) scale(0.8); opacity: 0; }
            20% { opacity: 0.1; } /* Adjusted max opacity */
            50% { transform: translateY(0px) rotate(10deg) scale(1); opacity: 0.1; }
            80% { opacity: 0.1; }
            100% { transform: translateY(-120px) rotate(-10deg) scale(0.8); opacity: 0; }
        }
        
        /* Icon Positions */
        .bg-icon:nth-child(1) { top: 10%; left: 5%; animation-duration: 18s; font-size: 4rem; }
        .bg-icon:nth-child(2) { top: 20%; right: 8%; animation-duration: 22s; font-size: 3.5rem; animation-delay: -5s; }
        .bg-icon:nth-child(3) { bottom: 20%; left: 10%; animation-duration: 16s; font-size: 4.5rem; animation-delay: -2s; }
        .bg-icon:nth-child(4) { bottom: 15%; right: 15%; animation-duration: 25s; }
        .bg-icon:nth-child(5) { top: 50%; left: 8%; animation-duration: 16s; font-size: 3rem; }
        .bg-icon:nth-child(6) { top: 70%; right: 35%; animation-duration: 19s; font-size: 2.5rem; animation-delay: -8s; }
        .bg-icon:nth-child(7) { bottom: 40%; left: 25%; animation-duration: 23s; font-size: 3.2rem; animation-delay: -3s; }

        .hero-content { position: relative; z-index: 3; }
        .hero h1 { font-size: clamp(2.5rem, 5vw, 5rem); margin-bottom: 1rem; line-height: 1.1; color: var(--pure-black); white-space: nowrap; width: 100%; font-weight: 900; }
        .hero h1 span { color: var(--primary-color); }
        .hero p { font-size: 1.2rem; color: var(--text-muted); margin-bottom: 2.5rem; font-weight: 500; }
        
        /* --- FIRE BUTTON STYLE --- */
        .fire-btn {
            display: inline-block; padding: 1rem 3rem; background-color: var(--primary-red); color: white;
            border-radius: 50px; font-weight: 800; text-transform: uppercase; letter-spacing: 1px;
            text-decoration: none; position: relative; z-index: 1; border: none;
            animation: burn-normal 1.5s infinite alternate ease-in-out; transition: all 0.3s ease;
        }
        .fire-btn::before {
            content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0;
            border-radius: 50px; background: linear-gradient(45deg, #ff0000, #ff4500);
            z-index: -1; opacity: 0; transition: opacity 0.3s;
        }
        @keyframes burn-normal {
            0% { box-shadow: 0 0 10px rgba(227, 28, 37, 0.5), 0 0 20px rgba(255, 69, 0, 0.3), inset 0 0 10px rgba(255, 255, 255, 0.1); transform: scale(1); }
            100% { box-shadow: 0 0 15px rgba(227, 28, 37, 0.7), 0 0 30px rgba(255, 69, 0, 0.5), inset 0 0 15px rgba(255, 255, 255, 0.2); transform: scale(1.02); }
        }
        @keyframes burn-intense {
            0% { box-shadow: 0 0 15px rgba(255, 0, 0, 0.8), 0 -5px 30px rgba(255, 69, 0, 0.7), 0 -10px 40px rgba(255, 140, 0, 0.5); }
            50% { box-shadow: 0 0 20px rgba(255, 0, 0, 0.9), 0 -8px 40px rgba(255, 69, 0, 0.8), 0 -15px 50px rgba(255, 165, 0, 0.6); }
            100% { box-shadow: 0 0 15px rgba(255, 0, 0, 0.8), 0 -5px 30px rgba(255, 69, 0, 0.7), 0 -10px 40px rgba(255, 140, 0, 0.5); }
        }
        .fire-btn:hover { animation: burn-intense 0.1s infinite alternate; background-color: #ff2a2a; transform: translateY(-2px); }

        /* --- SECTIONS --- */
        .section { padding: 6rem 0; margin: 0 auto; width: 100%; position: relative; overflow: hidden; }
        .section-container { max-width: 1200px; margin: 0 auto; padding: 0 2rem; z-index: 3; position: relative;}
        
        /* Updated Section Header with Chilli */
        .section-header { text-align: center; margin-bottom: 4rem; padding: 0 2rem;}
        .section-header h2 { font-size: 2.5rem; margin-bottom: 1rem; color: var(--pure-black); font-weight: 800; }
        
        .section-icon {
            font-size: 2rem;
            color: var(--primary-color);
            transform: rotate(-20deg);
            display: inline-block;
            filter: drop-shadow(0 2px 5px rgba(0,0,0,0.1));
        }

        /* --- DYNAMIC BG SECTION (Red/White Gradient) --- */
        .section-dynamic-bg {
            background: linear-gradient(-45deg, var(--bg-deep-red), var(--primary-red), #942727, var(--bg-deep-red));
            background-size: 400% 400%;
            animation: gradientBG 20s ease infinite;
            color: var(--pure-white);
        }
        @keyframes gradientBG {
            0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; }
        }
        .section-dynamic-bg .section-header h2 { color: var(--pure-white); }
        .section-dynamic-bg .section-icon { color: var(--pure-white); filter: drop-shadow(0 2px 5px rgba(0,0,0,0.2)); }

        /* --- SERVICES --- */
        .services-wrapper { width: 100%; overflow-x: auto; padding-bottom: 2rem; scrollbar-width: none; }
        .services-wrapper::-webkit-scrollbar { display: none; }
        .services-grid { display: flex; gap: 2rem; width: max-content; padding: 0 2rem; margin: 0 auto; }
        @media (min-width: 1400px) { .services-grid { width: 100%; justify-content: center; } }

        .service-card {
            min-width: 280px; width: 280px; background-color: var(--pure-white); padding: 2.5rem 1.5rem;
            border-radius: 8px; text-align: center; transition: var(--transition); border: 1px solid rgba(0,0,0,0.05);
            position: relative; display: flex; flex-direction: column; align-items: center; 
            box-shadow: 0 5px 15px rgba(0,0,0,0.05); z-index: 2;
        }
        .service-card:hover { transform: translateY(-5px); box-shadow: 0 15px 30px rgba(0,0,0,0.1); border-color: var(--primary-color); }
        .service-icon { font-size: 2.5rem; color: var(--primary-color); margin-bottom: 1.5rem; }
        .service-card h3 { margin-bottom: 1rem; font-size: 1.3rem; color: var(--pure-black); font-weight: 700; }
        .service-card p { color: var(--text-muted); font-size: 0.95rem; }

        /* --- CLIENTS (Infinite Scroll Fix) --- */
        .clients-section { background-color: var(--off-white); padding: 4rem 0; position: relative; overflow: hidden; }
        .scroller-container { 
            width: 100%; 
            overflow: hidden; 
            mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent); 
            -webkit-mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent); 
            z-index: 3; position: relative;
        }
        .scroller-track { 
            display: flex; 
            gap: 3rem; 
            width: max-content; 
            padding: 2rem 0; 
            animation: scrollLoop 30s linear infinite; 
        }
        .scroller-track:hover { animation-play-state: paused; }
        
        /* Crucial for seamless loop: We move -50%. 
           The HTML must contain 2 identical sets of items.
           When it reaches -50%, it snaps back to 0% instantly, which looks identical.
        */
        @keyframes scrollLoop { 
            0% { transform: translateX(0); } 
            100% { transform: translateX(-50%); } 
        }
        
        .client-logo-box { flex: 0 0 300px; height: 180px; background: var(--pure-white); border-radius: 8px; display: flex; align-items: center; justify-content: center; border: 1px solid var(--border-color); transition: var(--transition); box-shadow: 0 2px 8px rgba(0,0,0,0.02); z-index: 2; }
        .client-logo-box:hover { border-color: var(--primary-color); transform: scale(1.02); }

        /* --- FOUNDERS --- */
        .founders-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2rem; max-width: 900px; margin: 2rem auto 0; justify-items: center; padding: 0 2rem; }
        .founder-card { background-color: var(--pure-white); width: 100%; border-radius: 8px; overflow: hidden; transition: var(--transition); text-align: center; box-shadow: 0 10px 25px rgba(0,0,0,0.1); z-index: 2; border: none; }
        .founder-card:hover { transform: translateY(-5px); }
        .founder-img-placeholder { width: 100%; height: 250px; background-color: #eaeaea; display: flex; align-items: center; justify-content: center; color: #999; font-size: 4rem; }
        .founder-info { padding: 1.5rem; }
        .founder-info h3 { font-size: 1.5rem; margin-bottom: 0.5rem; color: var(--pure-black); }
        .founder-info .role { color: var(--primary-color); font-weight: 700; font-size: 0.9rem; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 1rem; display: block; }
        .founder-info p { font-size: 0.9rem; color: var(--text-muted); }

        /* --- CONTACT --- */
        .contact-container { display: grid; grid-template-columns: 1fr 1fr; gap: 4rem; padding: 0 2rem; }
        .contact-info h3 { font-size: 2.2rem; margin-bottom: 1rem; color: var(--pure-black); font-weight: 800; }
        .contact-item { display: flex; align-items: center; margin-bottom: 1.5rem; gap: 1rem; color: var(--dark-grey); }
        .contact-item i { color: var(--primary-color); font-size: 1.2rem; }
        .form-group { margin-bottom: 1.5rem; }
        .form-control { width: 100%; padding: 1rem; background-color: var(--off-white); border: 1px solid var(--border-color); border-radius: 4px; color: var(--pure-black); font-size: 1rem; transition: var(--transition); }
        .form-control:focus { outline: none; border-color: var(--primary-color); background-color: var(--pure-white); box-shadow: 0 0 0 2px rgba(166, 25, 46, 0.1); }
        
        /* --- FOOTER --- */
        footer { background-color: var(--pure-black); color: var(--pure-white); padding: 5rem 0 2rem; margin-top: 0; position: relative; z-index: 10; }
        .footer-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 3rem; margin-bottom: 3rem; }
        .footer-col h3 { font-size: 1.2rem; font-weight: 800; margin-bottom: 1.5rem; text-transform: uppercase; letter-spacing: 1px; color: var(--pure-white); }
        .footer-col p { font-size: 0.95rem; line-height: 1.6; margin-bottom: 1rem; color: #aaa; }
        .footer-col ul { display: flex; flex-direction: column; gap: 0.8rem; }
        .footer-col ul li a { font-size: 0.95rem; font-weight: 500; transition: var(--transition); color: #aaa; }
        .footer-col ul li a:hover { text-decoration: underline; padding-left: 5px; color: var(--primary-color); }
        .social-links { display: flex; gap: 1rem; }
        .social-box { width: 40px; height: 40px; border: 1px solid #333; display: flex; align-items: center; justify-content: center; color: #fff; transition: var(--transition); border-radius: 50%; }
        .social-box:hover { background-color: var(--primary-color); color: white; border-color: var(--primary-color); }
        .footer-bottom { border-top: 1px solid #333; padding-top: 2rem; text-align: center; font-size: 0.9rem; font-weight: 600; color: #666; }

        /* --- RESPONSIVE --- */
        @media (max-width: 768px) {
            .nav-links { position: absolute; right: 0; top: 60px; background-color: var(--pure-white); flex-direction: column; width: 100%; text-align: center; padding: 2rem 0; gap: 1.5rem; transform: translateY(-150%); z-index: 900; border-bottom: 1px solid var(--border-color); box-shadow: 0 10px 10px rgba(0,0,0,0.05); }
            .nav-links.active { transform: translateY(0); }
            .menu-toggle { display: block; }
            .contact-container { grid-template-columns: 1fr; gap: 2rem; }
            .founders-grid { grid-template-columns: 1fr; }
            
            .hero h1 { font-size: 2.8rem; }
            
            /* Reduced Icon Sizes for Mobile */
            .bg-icon { font-size: 1.5rem !important; } 
            .adobe-box { width: 40px; height: 40px; font-size: 1rem !important; border-radius: 6px; }
            
            .client-logo-box { flex: 0 0 200px; height: 120px; }
        }

        @media (max-width: 480px) {
            .section-header h2 { font-size: 2rem; }
            .footer-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <header>
        <nav>
            <div class="logo">
                <i class="fas fa-pepper-hot"></i> Mirapakaya <span>Media</span>
            </div>
            <div class="menu-toggle" id="mobile-menu"><i class="fas fa-bars"></i></div>
            <ul class="nav-links">
                <li><a href="#home">Home</a></li>
                <li><a href="#services">Services</a></li>
                <li><a href="#clients">Clients</a></li>
                <li><a href="#founders">Founders</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
    </header>

    <section id="home" class="hero">
        <div class="hero-spotlights">
            <div class="spotlight spotlight-1"></div>
            <div class="spotlight spotlight-2"></div>
        </div>

        <div class="bg-animated-icons">
            <i class="fab fa-youtube bg-icon"></i>
            <i class="fab fa-instagram bg-icon"></i>
            <i class="fab fa-meta bg-icon"></i>
            <div class="adobe-box adobe-pr bg-icon">Pr</div>
            <div class="adobe-box adobe-ae bg-icon">Ae</div>
            <i class="fas fa-clapperboard bg-icon"></i>
            <i class="fas fa-camera bg-icon"></i>
            <i class="fas fa-pepper-hot bg-icon"></i>
        </div>

        <div class="hero-content">
            <h1>Your Brand <span>= Our Spice</span></h1>
            <p>Spicing up the Digital World. No more boring marketing.</p>
            <p> We tell your story with heat.</p>
            <a href="#contact" class="fire-btn">Start The Fire</a>
        </div>
    </section>

    <section id="services" class="section section-dynamic-bg">
        <div class="bg-animated-icons">
            <i class="fas fa-film bg-icon"></i>
            <i class="fas fa-bullhorn bg-icon"></i>
            <i class="fas fa-share-nodes bg-icon"></i>
            <i class="fas fa-wand-magic-sparkles bg-icon"></i>
            <i class="fas fa-video bg-icon"></i>
            <i class="fas fa-laptop-code bg-icon"></i>
        </div>

        <div class="section-container">
            <div class="section-header">
                <h2>Our Services</h2>
                <i class="fas fa-pepper-hot section-icon"></i>
            </div>
        </div>

        <div class="services-wrapper">
            <div class="services-grid">
                <div class="service-card">
                    <i class="fas fa-video service-icon"></i>
                    <h3>Short Film Production</h3>
                    <p>Concept to screen. We handle scripting, filming, and direction for compelling short films.</p>
                </div>
                <div class="service-card">
                    <i class="fas fa-bullhorn service-icon"></i>
                    <h3>Influencer Marketing</h3>
                    <p>High-impact commercials and promo videos designed to elevate your brand identity.</p>
                </div>
                <div class="service-card">
                    <i class="fas fa-share-nodes service-icon"></i>
                    <h3>Social Media Marketing</h3>
                    <p>Strategic content creation and management to grow your digital presence.</p>
                </div>
                <div class="service-card">
                    <i class="fas fa-wand-magic-sparkles service-icon"></i>
                    <h3>Editing Services</h3>
                    <p>Professional post-production, color grading, and visual effects for all video types.</p>
                </div>
                <div class="service-card">
                    <i class="fas fa-chart-line service-icon"></i>
                    <h3>Performance Marketing</h3>
                    <p>Data-driven ad campaigns on Facebook, Instagram, and Google to maximize your ROI.</p>
                </div>
                <div class="service-card">
                    <i class="fas fa-laptop-code service-icon"></i>
                    <h3>Website Development</h3>
                    <p>We build responsive, visually stunning websites that represent your brand and convert visitors.</p>
                </div>
            </div>
        </div>
    </section>

    <section id="clients" class="clients-section">
        <div class="bg-animated-icons">
             <i class="fas fa-handshake bg-icon"></i>
             <i class="fas fa-users bg-icon"></i>
             <i class="fas fa-star bg-icon"></i>
        </div>

        <div class="section-container">
            <div class="section-header">
                <h2>Trusted By</h2>
                <i class="fas fa-pepper-hot section-icon"></i>
                <p style="color: var(--text-muted); margin-top: 10px;">Collaborating with visionary partners</p>
            </div>
        </div>
        <div class="scroller-container">
            <div class="scroller-track">
                <div class="client-logo-box"><img src="buchibabu.jpg" alt="Client 1" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="dharmic_minds.jpg" alt="Client 2" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="aahwanam logo new.png" alt="Client 3" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="buchibabu.jpg" alt="Client 1" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="dharmic_minds.jpg" alt="Client 2" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="aahwanam logo new.png" alt="Client 3" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                
                <div class="client-logo-box"><img src="buchibabu.jpg" alt="Client 1" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="dharmic_minds.jpg" alt="Client 2" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="aahwanam logo new.png" alt="Client 3" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="buchibabu.jpg" alt="Client 1" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="dharmic_minds.jpg" alt="Client 2" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
                <div class="client-logo-box"><img src="aahwanam logo new.png" alt="Client 3" style="max-width: 80%; max-height: 80%; object-fit: contain;"></div>
            </div>
        </div>
    </section>

    <section id="founders" class="section section-dynamic-bg">
        <div class="bg-animated-icons">
             <i class="fas fa-camera bg-icon"></i>
             <i class="fas fa-pen-nib bg-icon"></i>
             <i class="fas fa-lightbulb bg-icon"></i>
        </div>
        <div class="section-container">
            <div class="section-header">
                <h2>Meet The Visionaries</h2>
                <i class="fas fa-pepper-hot section-icon"></i>
            </div>
            <div class="founders-grid">
                <div class="founder-card">
                    <div class="founder-img-placeholder"><i class="fas fa-user"></i></div>
                    <div class="founder-info"><h3>Rathan Guntuka</h3><span class="role">Co-Founder</span><p>Creative director with a passion for storytelling and cinematic excellence.</p></div>
                </div>
                <div class="founder-card">
                    <div class="founder-img-placeholder"><i class="fas fa-user-tie"></i></div>
                    <div class="founder-info"><h3>Karthik Guntuka</h3><span class="role">Co-Founder</span><p>Strategic thinker ensuring every project meets the highest production standards.</p></div>
                </div>
            </div>
        </div>
    </section>

    <section id="contact" class="section">
        <div class="section-container">
            <div class="contact-container">
                <div class="contact-info">
                    <h3>Let's Spice It Up</h3>
                    <p style="color: var(--text-muted); margin-bottom: 2rem;">Ready to add some heat to your content? Reach out to Mirapakaya Media today.</p>
                    <div class="contact-item"><i class="fas fa-envelope"></i><span>rathanguntuka2@gmail.com</span></div>
                    <div class="contact-item"><i class="fas fa-phone"></i><span>+91 9014457199</span></div>
                    <div class="contact-item"><i class="fas fa-location-dot"></i><span>Hyderabad, India</span></div>
                </div>
                <form class="contact-form" onsubmit="event.preventDefault(); alert('Thanks for contacting Mirapakaya Media!');">
                    <div class="form-group"><input type="text" class="form-control" placeholder="Your Name" required></div>
                    <div class="form-group"><input type="email" class="form-control" placeholder="Your Email" required></div>
                    <div class="form-group"><textarea class="form-control" rows="5" placeholder="Project Details" required></textarea></div>
                    <button type="submit" class="fire-btn">Send Message</button>
                </form>
            </div>
        </div>
    </section>

    <footer>
        <div class="section-container">
            <div class="footer-grid">
                <div class="footer-col">
                    <h3>MIRAPAKAYA MEDIA</h3>
                    <p>We Create Visual Masterpieces. We dedicate ourselves to telling your story with cinematic excellence, strategic impact, and a touch of spice.</p>
                </div>
                
                <div class="footer-col">
                    <h3>Quick Links</h3>
                    <ul>
                        <li><a href="#home">Home</a></li>
                        <li><a href="#services">Services</a></li>
                        <li><a href="#clients">Clients</a></li>
                        <li><a href="#founders">Founders</a></li>
                        <li><a href="#contact">Contact</a></li>
                    </ul>
                </div>

                <div class="footer-col">
                    <h3>Our Services</h3>
                    <ul>
                        <li><a href="#services">Short Films</a></li>
                        <li><a href="#services">Brand Advertisements</a></li>
                        <li><a href="#services">Social Media Marketing</a></li>
                        <li><a href="#services">Editing & VFX</a></li>
                        <li><a href="#services">Website Development</a></li>
                    </ul>
                </div>

                <div class="footer-col">
                    <h3>Get Social</h3>
                    <p>Follow us for updates and behind-the-scenes content.</p>
                    <div class="social-links">
                        <a href="#" class="social-box"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="social-box"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="social-box"><i class="fab fa-youtube"></i></a>
                        <a href="#" class="social-box"><i class="fab fa-twitter"></i></a>
                    </div>
                </div>
            </div>

            <div class="footer-bottom">
                <p>&copy; 2026 Mirapakaya Media. All Rights Reserved.</p>
            </div>
        </div>
    </footer>

    <script>
        // --- Menu & Scroll Reveal ---
        const menuToggle = document.getElementById('mobile-menu');
        const navLinks = document.querySelector('.nav-links');
        const links = document.querySelectorAll('.nav-links li');
        menuToggle.addEventListener('click', () => { navLinks.classList.toggle('active'); const icon = menuToggle.querySelector('i'); if(navLinks.classList.contains('active')) { icon.classList.remove('fa-bars'); icon.classList.add('fa-times'); } else { icon.classList.remove('fa-times'); icon.classList.add('fa-bars'); } });
        links.forEach(link => { link.addEventListener('click', () => { navLinks.classList.remove('active'); const icon = menuToggle.querySelector('i'); icon.classList.remove('fa-times'); icon.classList.add('fa-bars'); }); });

        const observer = new IntersectionObserver((entries) => { entries.forEach(entry => { if (entry.isIntersecting) { entry.target.style.opacity = '1'; entry.target.style.transform = 'translateY(0)'; } }); }, { threshold: 0.1 });
        document.querySelectorAll('.service-card, .founder-card').forEach((el) => { el.style.opacity = '0'; el.style.transform = 'translateY(20px)'; el.style.transition = 'all 0.6s ease-out'; observer.observe(el); });
    </script>
</body>
</html>
